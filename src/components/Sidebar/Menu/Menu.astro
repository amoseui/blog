---
import { href } from "@/lib/urls";

import styles from "./Menu.module.scss";

interface Props {
  menu: Array<{
    label: string;
    path: string;
  }>;
}

const { menu } = Astro.props;

// Marks the menu item whose link exactly matches the current page path,
// normalized to the trailing-slash link form.
const currentPath = href(Astro.url.pathname);
---

<nav class={styles.menu}>
  <ul class={styles.list}>
    {
      menu.map((item) => (
        <li class={styles.item}>
          <a
            href={href(item.path)}
            class:list={[
              styles.link,
              { [styles.active]: currentPath === href(item.path) },
            ]}
          >
            {item.label}
          </a>
        </li>
      ))
    }
  </ul>
</nav>
