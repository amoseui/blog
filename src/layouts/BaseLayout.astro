---
import "@/assets/scss/main.scss";

import Meta from "@/components/Meta/Meta.astro";
import styles from "@/components/Layout/Layout.module.scss";
import { site } from "@/lib/site";

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { title, description = site.subtitle, image } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <Meta title={title} description={description} image={image} />
    <link rel="icon" href="/favicon-32x32.png?v=15add44ca6493b05c7e04dcb785a9707" type="image/png"/>
    <link rel="apple-touch-icon" sizes="48x48" href="/icons/icon-48x48.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <link rel="apple-touch-icon" sizes="72x72" href="/icons/icon-72x72.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <link rel="apple-touch-icon" sizes="96x96" href="/icons/icon-96x96.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <link rel="apple-touch-icon" sizes="144x144" href="/icons/icon-144x144.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <link rel="apple-touch-icon" sizes="192x192" href="/icons/icon-192x192.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <link rel="apple-touch-icon" sizes="256x256" href="/icons/icon-256x256.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <link rel="apple-touch-icon" sizes="384x384" href="/icons/icon-384x384.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <link rel="apple-touch-icon" sizes="512x512" href="/icons/icon-512x512.png?v=15add44ca6493b05c7e04dcb785a9707"/>
    <script is:inline>
      (() => {
        let mode = "light";
        try {
          const stored = JSON.parse(localStorage.getItem("diesel:theme-atom"));
          if (stored && (stored.mode === "dark" || stored.mode === "light"))
            mode = stored.mode;
          else if (matchMedia("(prefers-color-scheme: dark)").matches)
            mode = "dark";
        } catch (_) {
          if (matchMedia("(prefers-color-scheme: dark)").matches) mode = "dark";
        }
        document.documentElement.className = mode;
      })();
    </script>
    <script
      is:inline
      async
      src={`https://www.googletagmanager.com/gtag/js?id=${site.googleAnalyticsId}`}
    ></script>
    <script is:inline define:vars={{ gaId: site.googleAnalyticsId }}>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());
      gtag("config", gaId);
    </script>
  </head>
  <body>
    <div class={styles.layout}><slot /></div>
  </body>
</html>
